% -- author: Alan Livio <alan@telemidia.puc-rio.br>
% -- URL:    https://github.com/alanlivio/latex-helper-functions

\newcommand\hfEnableCommonDocument{
  % -- layout
  % \RequirePackage[a4paper, total={6in, 9.5in}]{geometry}
  \RequirePackage{wordlike}

  % -- document specifics
  \RequirePackage[brazilian, american]{babel}

  % -- tables
  \RequirePackage[table]{xcolor}
  \RequirePackage{multirow}
  \RequirePackage{float}
  \RequirePackage{makecell}
  \RequirePackage{tabularx}
  \RequirePackage{multirow}

  % -- bib
  \RequirePackage[numbers,sort&compress]{natbib}
  \setlength{\bibsep}{0pt}
  \RequirePackage{usebib}

  % -- fonts
  \RequirePackage{lmodern}
  \RequirePackage[T1]{fontenc}
  \RequirePackage[utf8]{inputenc}
  \RequirePackage{textcomp} %  text symbols
  \RequirePackage{amsmath,amssymb,amsfonts} % math

  % -- svg
  \RequirePackage{svg}

  % -- tables
  \RequirePackage{makecell}
  \RequirePackage{tabularx}
  \RequirePackage{multirow}

  % -- itemize
  \def\labelitemi{—}
  \RequirePackage{enumitem}
  \SetEnumitemKey{mynosep}{noitemsep, nosep, topsep=0pt, partopsep=0pt, parsep=0pt, itemsep=0pt, leftmargin=*,rightmargin=0pt}
  \def\labelitemi{$-$}

  % -- URL with hyperref
  \RequirePackage{url}
  \RequirePackage{hyperref}
  \hypersetup{hidelinks}

  % -- includegraphics
  \RequirePackage{graphicx}

  % -- includegraphics
  \RequirePackage{color,soul}
  \RequirePackage[nomargin,inline]{fixme}
  \RequirePackage{subcaption} %p pra organizar melhor as imagens

  % -- lipsum
  \RequirePackage{lipsum}
}

\newcommand\hfReduceSpacing{
    % -- reduce section titles spacing
    \RequirePackage[compact]{titlesec}
    \titlespacing{\section}{0pt}{*0}{*0}
    \titlespacing{\subsection}{0pt}{*0}{*0}
    \titlespacing{\subsubsection}{0pt}{*0}{*0}

    % -- reduce itemize spacing
    \setlength{\itemsep}{0pt}

    % -- reduce paragraph spacing
    \setlength{\parskip}{0pt}
    \setlength{\parsep}{0pt}

    % -- reduce caption spacing
    \setlength{\abovecaptionskip}{1pt}
    \setlength{\belowcaptionskip}{1pt}
}

% -- draft watermark
\newcommand\hfEnableDraftWatermark{
  \RequirePackage{draftwatermark}
  \SetWatermarkFontSize{0.5in}
  \SetWatermarkColor[gray]{.9}
  \SetWatermarkText{\parbox{12em}{\centering
      Unpublished working draft.\\
      Not for distribution.}}
}

% -- acm prepring
\newcommand\hfEnableACMPreprint{
  \settopmatter{printacmref=false}
  \setcopyright{none}
  \renewcommand\footnotetextcopyrightpermission[1]{}
  \pagestyle{plain}
}

% -- blind review authors
\newcommand\hfAddACMBlindReviewAuthors{
  \author{Removed for double-blind review}
  \affiliation{%
    \institution{Removed for double-blind review}}
  \author{Removed for double-blind review}
  \affiliation{%
    \institution{Removed for double-blind review}}
  \author{Removed for double-blind review}
  \affiliation{%
    \institution{Removed for double-blind review}}
  \renewcommand{\shortauthors}{Removed for double-blind review}
}

\newcommand\hfAddIEEEBlindReviewAuthors{
  \author{
  \IEEEauthorblockN{Removed for double-blind review}
  \IEEEauthorblockA{Removed for double-blind review  \\ Removed for double-blind review}
  \and
  \IEEEauthorblockN{Removed for double-blind review}
  \IEEEauthorblockA{Removed for double-blind review  \\ Removed for double-blind review}
  \and
  \IEEEauthorblockN{Removed for double-blind review}
  \IEEEauthorblockA{Removed for double-blind review  \\ Removed for double-blind review}
  }
}
